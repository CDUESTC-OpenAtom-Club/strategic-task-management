/**
 * Vue Application Entry Point
 *
 * This is the root entry point that delegates to app layer
 * Following FSD (Feature-Sliced Design) architecture
 *
 * **Validates: Requirements 3.1 - Application Layer**
 */

// Import and execute application initialization from app layer
import './app/main'
